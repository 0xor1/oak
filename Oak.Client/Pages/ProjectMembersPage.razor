@page "/org/{OrgId}/project/{ProjectId}/members"
@using Oak.I18n
@using Common.Client
@inject L L;
<PageTitle>@L.S(S.ProjectMembers)</PageTitle>
<ProjectMembers OrgId="@OrgId" ProjectId="@ProjectId"/>

@code{

    [Parameter]
    public string OrgId { get; set; }
    [Parameter]
    public string ProjectId { get; set; }

}