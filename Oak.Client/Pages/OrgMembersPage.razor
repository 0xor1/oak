@page "/org/{OrgId}/members"
@using Oak.I18n
@using Common.Client
@inject L L;
<PageTitle>@L.S(S.OrgMembers)</PageTitle>
<OrgMembers OrgId="@OrgId"/>

@code{

    [Parameter]
    public string OrgId { get; set; }

}