namespace Oak.Client.Lib;

public static class Util
{
    public static void Time(ulong val, uint? hoursPerDay, uint? daysPerWeek)
    {
        if (hoursPerDay > 0 && daysPerWeek > 0)
        {
            var weeksPerYear = 52ul;
            // render as #y #w #d #h #m
        }
    }
}
